[build]
  command = "npm --prefix dc20-web ci && npm --prefix dc20-web run build"
  publish = "dc20-web/dist"

[build.environment]
  NODE_VERSION = "22"
  # Firebase publishes this placeholder in its type documentation. It is not a credential.
  SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES = "AIzaSyDOCAbC123dEf456GhI789jKl012-MnO"

[context.production]
  command = "npm --prefix dc20-web ci && npm --prefix dc20-web run build"

[context.deploy-preview]
  command = "npm --prefix dc20-web ci && npm --prefix dc20-web run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/data/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
